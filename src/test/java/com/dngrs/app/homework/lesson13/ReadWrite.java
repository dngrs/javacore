package com.dngrs.app.homework.lesson13;

/**
 * Created by I. Odokienko on 11/17/16.
 */
public class ReadWrite {
    /*
    public static void main(String[] args) {
        String fileName = ""
    }

    public static BufferedReader readFromFileToBuffer(String fileName) throws IOException{
            BufferedReader buffer = new BufferedReader(new FileReader(fileName));
            buffer.close();
            return buffer;
    }

    public static ArrayList[]<Integer> readStringsFromFile(String path) throws IOException {
        ArrayList[]<Integer> result = new ArrayList<>();
        BufferedReader reader = new BufferedReader(new FileReader(path));
        String line;
        while ((line = reader.readLine()) != null) {
            String[] numbers = line.split(",");
            result.add(fromStringArray(numbers));
        }
        reader.close();
        return result;
    }

    public static int[] fromStringArray(String[] array) {
        int[] result = new int[array.length];
        for (int i = 0; i < array.length; i++) {
            result[i] = Integer.valueOf(array[i]);
        }
        return result;
    }

    */
}

